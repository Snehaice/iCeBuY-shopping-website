const e=document.getElementById("loginForm"),t=document.getElementById("signupForm"),s=document.getElementById("passwordMsg");t.addEventListener("submit",n=>{n.preventDefault();const o=document.getElementById("signupPassword").value,d=document.getElementById("confirmPassword").value;/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/.test(o)?o===d?(alert("Signup successful (frontend only)"),t.reset(),e.classList.toggle("active"),t.classList.toggle("active")):s.textContent="Passwords do not match":s.textContent="Password must be 8+ chars, include uppercase, lowercase & number"}),e.addEventListener("submit",t=>{t.preventDefault(),alert("Login successful (frontend only)"),e.reset()});